package moller.javapeg.program.enumerations;

/**
 * Indicates whether a File object (Which does not exist in a file system, yet)
 * is a file or a directory.
 * 
 * @author Fredrik
 */
public enum Type {
	FILE,
	DIRECTORY
}
